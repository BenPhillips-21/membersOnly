extends layout

block content

  form(method='POST')
    div.form-group
      label(for='adminKey') Enter the Admin Key:
      input#adminKey.form-control(type='text', placeholder="..." name='adminKey' required )
      br
      button.btn.btn-primary(type='submit') Submit

  if errors
    ul
      for error in errors
        li!= error.msg