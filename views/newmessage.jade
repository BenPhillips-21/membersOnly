extends layout

block content

  form(method='POST')
    div.form-group
      label(for='message') Enter Your Message:
      input#message.form-control(type='text', placeholder="..." name='message' required )
      br
      button.btn.btn-primary(type='submit') Submit

  if errors
    ul
      for error in errors
        li!= error.msg
